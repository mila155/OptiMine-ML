version: '3.8'

services:
  optimine-api:
    build: .
    container_name: optimine-api
    ports:
      - "8000:8000"
    environment:
      - MODELS_DIR=/app/models
      - GROQ_API_KEY=${GROQ_API_KEY}
    volumes:
      - ./models:/app/models
      - ./app:/app/app
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s